<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/firstView">
    <fragment
        android:id="@+id/firstView"
        android:name="com.endurance.medkids.ui.login.FirstView"
        android:label="FirstView" >
        <action
            android:id="@+id/action_firstView_to_loadingView"
            app:destination="@id/loadingView" />
    </fragment>
    <fragment
        android:id="@+id/loadingView"
        android:name="com.endurance.medkids.ui.login.LoadingView"
        android:label="fragment_loading_view"
        tools:layout="@layout/fragment_loading_view" />
</navigation>